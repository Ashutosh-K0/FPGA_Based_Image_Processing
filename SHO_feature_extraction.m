function extracted_features = SHO_feature_extraction(img)
    % 📌 Selfish Herd Optimization Feature Extraction Algorithm
    % Input:  img  - Grayscale Image (256x256)
    % Output: extracted_features - Feature Vector
    
    % Example Feature Extraction - Replace this with the actual SHO algorithm
    extracted_features = extractHOGFeatures(img);  % Using HOG as a placeholder
    
    % If SHO uses multiple feature extraction steps, combine them
    % extracted_features = [feature1, feature2, feature3, ...];
    
end
